[project]
name = "haruhi-roleplay-api"
version = "0.1.0"
description = "Roleplay API orchestration service for character presets, session, RAG, and memory."
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[tool.uv]
package = false
